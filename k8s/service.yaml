apiVersion: v1
kind: Service
metadata:
  name: xstream-backend-svc
  labels:
    app: xstream-backend
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8000"
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: xstream-backend